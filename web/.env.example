# The Django backend. Every clinical decision — Claude summarisation, the
# interval plan, the red-flag cluster count, the brief — happens there, so
# this app needs it running. Defaults to localhost:8000 when unset.
NEXT_PUBLIC_API_BASE=http://localhost:8000
