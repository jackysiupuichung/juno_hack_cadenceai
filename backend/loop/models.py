# Intentionally no Django ORM models here — Supabase Postgres is the data
# store, reached via PostgREST (see supabase_client.py, repo.py). Table
# shapes live in supabase/migrations/, not Django migrations.
